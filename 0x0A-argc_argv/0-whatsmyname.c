#include <stdio.h>
/**
 * main - the main ftn
 * @argc: parameter for the no. of argument passed in terminal
 * argv: parameter for displaying the args passed in the terminal
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n",*argv);
	return (0);
}
